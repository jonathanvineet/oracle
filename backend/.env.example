# Example environment variables for the backend
JWT_SECRET=replace_with_a_long_random_secret
JWT_EXPIRES_IN=1h
# Optional: enable Vercel Blob by setting VERCEL_BLOB_TOKEN and BUCKET
VERCEL_BLOB_TOKEN=
BUCKET=
