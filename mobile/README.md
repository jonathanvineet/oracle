# Oracle Mobile (Expo)

This is the Expo app that streams camera frames to the ORACLE backend.

Quick start:

```bash
cd mobile
npm install
npx expo start
```

Set `BACKEND_URL` in environment or replace `BACKEND` in the code for testing.
